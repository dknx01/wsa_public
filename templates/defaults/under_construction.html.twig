{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron justify-content-center">
        <div class="card mb-3" style="max-width: 540px;">
            <div class="row g-0">
                <div class="col-md-4">
                    <img src="{{ asset('images/4951b69b-3294-4adb-917a-c0b1bd446709.jpg') }}" class="img-fluid rounded-start" alt="an error occurred">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">{{ 'under_construction.title'|trans({}, 'error') }}</h5>
                        <p class="card-text">
                        <p>{{ 'under_construction.first_paragraph'|trans({}, 'error') }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
